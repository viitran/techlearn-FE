export function checkRole({ store, next }) {
  // const role = store.getters.getUserRole;

  // if (role && role === "gvT") {
  //   next();
  // } else {
  //   next({ name: "StudentList" });
  // }
}
